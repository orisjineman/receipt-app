# --- Basic Auth (단일 사용자) ---
# 백엔드의 BASIC_AUTH_USER / BASIC_AUTH_PASSWORD 와 반드시 동일해야 함.
BASIC_AUTH_USER="me"
BASIC_AUTH_PASSWORD="여기에-충분히-긴-비밀번호 (16자 이상 권장)"

# --- Backend URL (next.config.js rewrites 가 사용) ---
# 로컬은 4000, 배포는 백엔드 Vercel 프로젝트 도메인.
BACKEND_URL="http://localhost:4000"
